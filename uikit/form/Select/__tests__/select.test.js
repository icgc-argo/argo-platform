import React from 'react';
import { render } from '@testing-library/react';
import { RefactoredSelect } from '../index';

describe('Select', () => {
  it('renders focusable button', () => false);

  it('toggles dropdown on click', () => false);

  it('toggles dropdown on enter', () => false);

  it('closes dropdown on option select', () => false);

  it('closes when looses focus', () => false);

  it('selects options with keyboard', () => false);
});
